version: 2.1
orbs:
  elixir: membraneframework/elixir@1

workflows:
  version: 2
  build:
    jobs:
      - elixir/build_test:
          name: build_test1
          filters: &filters
            tags:
              only: /v.*/
      - elixir/build_test:
          name: build_test2
          filters: &filters
            tags:
              only: /v.*/
      - elixir/test:
          requires:
            - build_test2
          filters:
            <<: *filters
      - elixir/lint:
          filters:
            <<: *filters
          docs: false
      - elixir/hex_publish:
          requires:
            - build_test2
            - elixir/test
            - elixir/lint
          context:
            - Deployment
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /v.*/
